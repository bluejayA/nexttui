# NFR Requirements

**Timestamp**: 2026-03-23T09:00:00+09:00
**Mode**: GENERATE
**Domain**: D (사내 도구) + 보안 상향 — 프로덕션 클라우드 인프라 직접 제어
**Profile**: B (소규모 운영) — 사용자 100명 이하, KT Cloud 운영팀 내부

## NFR Summary

| # | 카테고리 | 요구사항 | 근거 |
|---|---------|---------|------|
| 1 | 성능 | 키 입력→화면 갱신 < 16ms (60 FPS), API 호출 중 UI 블로킹 0ms (완전 비동기), 1,000개 리소스 리스트 렌더링 < 50ms, 메모리 사용량 < 50MB | 로컬 TUI — 서버 처리량 불필요, UI 반응성이 핵심. ratatui 기반 터미널 렌더링 성능 기준 |
| 2 | 보안 | clouds.yaml 패스워드/시크릿 메모리 전용 유지 (로그 출력 금지), TLS 인증서 검증 기본 활성화 (insecure 옵션 시에만 비활성화), 감사 로그에 민감 정보(패스워드, 토큰) 기록 금지, Keystone 역할 기반 RBAC 메뉴/액션 가시성 제어, CUD 작업 로컬 감사 로그 기록, 고위험 작업(삭제/강제변경/마이그레이션) 2단계 확인 (리소스명 재입력) | 사내 도구 기준 + 보안 상향: 프로덕션 인프라 직접 제어이므로 감사 추적·권한 제어 필수. Phase 2에서 중앙 RBAC/감사 시스템 연동 |
| 3 | 가용성 | 로컬 앱 크래시 시 진행 중 데이터 손실 없음 (커맨드 히스토리·감사 로그 즉시 flush), API 호출 실패 시 캐시된 데이터 표시 + 에러 알림, 인증 토큰 만료 시 자동 갱신 시도 → 실패 시 재인증 프롬프트, 네트워크 일시 단절 시 앱 크래시 없이 상태바에 연결 상태 표시 | 로컬 실행 앱이므로 서버 업타임 SLA 불필요. 에러 복원력(graceful degradation)에 집중 |
| 4 | 데이터 무결성 | 커맨드 히스토리 로컬 파일 저장 (`~/.config/nexttui/command_history`, 최대 50개), CUD 감사 로그 로컬 파일 저장 (`~/.config/nexttui/audit.log`), 캐시는 휘발성 (앱 재시작 시 API에서 재로드), CUD 작업 전 확인 다이얼로그 필수 | 로컬 TUI이므로 DB 백업/트랜잭션 불필요. 운영 실수 방지가 핵심 목적 |
| 5 | 배포/운영 | 단일 정적 바이너리 (musl 타겟 지원), macOS (aarch64, x86_64) + Linux (x86_64, aarch64) + Windows (x86_64), VDI 기반 관리망 내부 실행, 별도 런타임 의존성 없이 배포/회수 용이 | 운영팀 VDI 환경에서 관리망 접근. 단일 바이너리로 설치/제거 단순화 |

## 해당 없음 카테고리 (근거)

| 카테고리 | 제외 근거 |
|---------|---------|
| 확장성 | 로컬 실행 TUI — 수평/수직 확장 대상 아님 |
| 모니터링 | 서버 컴포넌트 없음 — APM/대시보드 불필요. 앱 내부 Toast 알림으로 충분 |
| 재해 복구 | 로컬 앱 — RPO/RTO 대상 아님. 재실행으로 복구 |
| 컴플라이언스 | 현 시점 외부 규제 대상 아님. Phase 2에서 중앙 감사 연동 시 재검토 |

## 테스트 요구사항 (개발 프로세스)

별도 관리 항목 — NFR이 아닌 개발 프로세스 요구사항:
- Port trait의 Mock 구현으로 API 없이 TUI 로직 단위 테스트 가능
- `#[cfg(test)]` 모듈 내 단위 테스트
- TDD 기반 개발 (CLAUDE.md 규칙)

## 조정 이력

없음 — 기본값 그대로 승인됨.
